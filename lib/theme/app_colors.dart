import 'package:flutter/material.dart';

class AppColors {
  static const black = Color(0xFF000000);

  static const kakaoGreen = Color(0xFF388E3C);     // 진초록
  static const splashGreen = Color(0xFFA0CD9A);  // 연초록 (스플래시 배경 등)

  static const heartRed = Color(0xFFF8486E);   // 강조 빨강 (하트, 포인트)
  static const accentTeal = Color(0xFF32BEA6);  // 밝은 청록 (버튼 강조 등)

  // 추가적으로 필요한 경우
  static const lightGray = Color(0xFFF9F6EC);   // 배경용 연베이지 등
  static const categroyGray = Color(0xFF959595); // 한식,중식 글씨 색
}
